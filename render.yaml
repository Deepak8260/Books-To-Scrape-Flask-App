services:
  - type: web
    name: books-toscrap-flask-app
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: "gunicorn app:app"


services:
  - type: web
    name: flask-portfolio
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
